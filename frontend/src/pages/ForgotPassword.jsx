import React,{useState} from 'react'; import api from '../api/axios';
export default function ForgotPassword(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const submit=async e=>{e.preventDefault();try{await api.post('/auth/forgot-password',{email});setMsg('If the account exists, a reset link has been sent.')}catch(e){setMsg('Request could not be completed.')}};return <div className="card" style={{maxWidth:500,margin:'4rem auto'}}><h1>Forgot Password</h1><form onSubmit={submit}><input className="input" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><button className="btn btn-primary" style={{marginTop:15}}>Send Reset Link</button></form><p>{msg}</p></div>}
